vpn:
    enabled: false
    # protocol: wireguard  # or openvpn, or leave empty for bind-only mode
    # interface: wg0       # bind-only mode: name of the VPN interface to bind to
    #
    # wireguard:
    #     private_key: YOUR_PRIVATE_KEY
    #     address: 10.x.x.x/32
    #     dns: 10.x.x.x
    #     peer_public_key: PEER_PUBLIC_KEY
    #     peer_endpoint: vpn.example.com:51820
    #     allowed_ips: 0.0.0.0/0
    #     persistent_keepalive: 25

servers: []
# Add NNTP servers via the web UI, or fill in here:
# - name: my-provider
#   host: news.example.com
#   port: 563
#   ssl: true
#   username: user
#   password: pass
#   connections: 20
#   enabled: true

paths:
    incomplete: /downloads/incomplete
    complete: /downloads/complete
    temp: /config/cache

web:
    port: 6789
    username: admin
    password: changeme

postprocess:
    delete_archives: true
